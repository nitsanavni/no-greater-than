# GOAL: all three no-greater-than tools are fair & correct across the corpus

- [x] confirm the corpus ground truth (real >/>= sites flagged by all three tools; TS-generic / JSX / shift / cast non-sites correctly ignored)
  - [x] reconciled site count: latest tri-tool review reports 13 genuine numeric >/>= comparisons flagged with zero false positives (supersedes earlier "8 sites" snapshot; corpus grew). All three tools agree on detection; no missed sites.
  - [x] fix-audit on a fresh 2-site snapshot (line 70 `code > 999`, line 576 `keys.length > 0`): both genuine violations, all three tools detect identically; every operand is a pure read (numeric param, integer literals, array `.length`). eslint (`999 < code`, `0 < keys.length`) and astgrep (`(999) < (code)`, `(0) < (keys.length)`) re-parsed cleanly, empty risky_fixes — fully behavior-preserving. Only diff is cosmetic paren-noise from astgrep.
- [x] eslint: detection correct (TS parser configured, all real sites flagged, generics ignored)
  - [x] eslint: improve fix coverage / documentation
    - [x] auto-fix when RHS is a known-pure builtin call (Date.parse etc.) — done via PURE_STATIC_CALLS allowlist (commit e2c14cc)
    - [x] confirm literal-on-left flips like '0 < x' match the number-line intent (current output correct)
    - [x] confirm swapped-operand fixes are clean & behavior-preserving across all 13 sites (tri-tool review: clean, no correctness defects)
  - [ ] eslint: optional polish (non-blocking; no current site triggers these)
    - [x] guard against swapping side-effecting operands: ALREADY IMPLEMENTED — isSideEffectFree() returns false for CallExpression (minus a pure-global allowlist), and check() emits fix:null + a suggestion when an operand has side effects under autofixSafeOnly (default true). Tests in tests/unit.test.js assert this. No source change needed (reconciled: skipped — re-implementing would duplicate existing behavior).
    - [x] suggestion text names the concrete safe rewrite: ALREADY IMPLEMENTED — the `flip` message is "Flip to '{{rewrite}}'" and `buildRewrite()` produces the exact swapped form (e.g. `startOfNextYear.getTime() <= _date.getTime()`), so the suggestion already spells out the one-keystroke rewrite. No change needed.
    - [x] extend pure-call allowlist to known-pure INSTANCE getters (Date.prototype getTime/valueOf/getFullYear/getMonth/getDate/getHours/getMinutes/getSeconds/getTime-family, etc.) so `a.getTime() >= b.getTime()` PROMOTES from suggestion-only to autofix — DONE (commit 48b2a714912e064b246b0f562d4103bd3222b032): pure-call allowlist now matches known-pure instance getters, so `a.getTime() >= b.getTime()` autofixes to `b.getTime() <= a.getTime()` (behavior-preserving, both reads pure)
    - [ ] preserve original spacing/comments around the operator in fix output for minimal diffs @leaf
    - [x] extend pure-call allowlist to known-pure Array/String READ methods (indexOf, lastIndexOf, includes, charAt, slice) so safe call-operand comparisons like `urls.indexOf(x) >= urls.length - 1` PROMOTE from suggestion-only to autofix — DONE (commit 5ac0080488e0fa4ba6278592484ee5170be8c317): PURE_INSTANCE_METHODS extended with indexOf/lastIndexOf/includes/charAt/slice (read-only on Array/String/TypedArray, so safe under name-only matching); conservative side-effect-free-receiver + name-only-match guards kept
    - [x] keep the call-operand side-effect guard as the default for impure cases (call may be impure in general) — ALREADY IMPLEMENTED: isSideEffectFree() returns false for un-allowlisted CallExpression, forcing suggestion-only under autofixSafeOnly
    - [x] VERIFIED on `getObserversCount() > 0` snapshot: `getObserversCount` is not in PURE_INSTANCE_METHODS, so isPureInstanceCall()→false → isSideEffectFree(call)→false → all three sites (282/294/307) correctly downgrade to suggestion-only with the concrete `flip` rewrite `0 < this.getObserversCount()` spelled out; the pure `dataUpdateCount + errorUpdateCount > 0` site (290) is a BinaryExpression of member reads (isSideEffectFree=true) so it autofixes to `0 < (this.state.dataUpdateCount + this.state.errorUpdateCount)`. No source change needed.
  - [x] eslint: emit clean swaps with no parens for atomic operands (identifier/literal/member), minimal parens only when precedence requires — ALREADY IMPLEMENTED via operandText()/NEEDS_PARENS; verified on new snapshot (`999 < code`, `0 < keys.length`)
  - [x] eslint: detection has no correctness gaps (tri-tool review): all 6 real comparisons flagged (217, 275, 308, 432, 442, 443); decoy JSDoc generics (33/34) and JSX-in-comment (220/221) correctly ignored; zero false positives/negatives
    - [x] line 432 correctly downgrades to suggestion-only (ternary RHS has lower precedence than the comparison) — keep this conditional behavior
    - [x] >= cases (442, 443) flip to <= (e.g. 0 <= x) — behavior-preserving and correct
  - [ ] eslint: clean up unrelated config noise — the corpus file's own `/* eslint-disable @typescript-eslint/no-redundant-type-constituents */` references a rule not loaded by the minimal ngt config, producing a line-1 error. ENVIRONMENTAL/HARNESS: not fixable in the rule source (eslint/rules/no-greater-than.js); requires either loading the TS plugin rule or scoping the ngt config so the disable comment doesn't error. Low confidence; harness-level @leaf
- [ ] biome: reach parity with eslint
  - [x] biome: detection correct (native TS parsing, all real sites flagged, no generic false positives)
  - [!] biome: implement autofix/rewrite — BLOCKED: Biome v2 GritQL plugins are detect-only (framework limitation, not ours). Do not add rewrite leaves.
  - [x] biome: keep GritQL pattern targeting only relational binary expressions (already confirmed)
  - [x] biome: detection complete & precise (tri-tool review) — all 6 real comparisons flagged, zero false positives on JSDoc generics (33/34) and JSX-in-comment (220/221); no precision changes needed for this file
  - [x] biome: keep detect-only — GritQL plugins in Biome v2 cannot rewrite (do not add autofix)
  - [x] biome: confirmed as third detection oracle — flagged the same sites as eslint/astgrep on the new snapshot (detection parity)
  - [ ] biome: enrich the detect-only diagnostic — message already shows the generic before→better template (`a > b` → `b < a`, `a >= b` → `b <= a`), but does NOT explain WHY Biome won't auto-apply when operands are method calls. Add a note like "not auto-fixed because both sides may be method calls (eval-order)" so the reader understands the conservatism. NOTE: GritQL cannot interpolate the matched operands, so the concrete per-site rewrite (e.g. `startOfNextYear.getTime() <= _date.getTime()`) cannot be printed — only a static template + explanation is achievable @leaf
  - [ ] biome: consider lowering the pure-flip diagnostic from error to warning so a purely stylistic finding doesn't force a non-zero exit (summary.errors=1) — unless the project intends it as a hard gate. CAVEAT: Biome v2 GritQL `register_diagnostic` emits at error severity with no per-call severity argument, so this may not be expressible inside the .grit plugin and may require a framework-level option; verify before attempting. Low confidence @leaf
  - [ ] biome: optional precision hardening as the file grows — keep excluding `<T extends ...>` type-parameter lists, JSX tags, and bit-shift (>> / >>>) contexts; GritQL pattern must continue to exclude shift operators and TS type-argument < > (current output already does) @leaf
- [x] fix-audit on a fresh 4-site snapshot (line 290 `dataUpdateCount + errorUpdateCount > 0`; lines 282/294/307 `getObserversCount() > 0`): all four are genuine `>` sites; every applied autofix is behavior-preserving. eslint & astgrep each auto-applied ONLY line 290 (two pure operands: a `+` of member reads vs literal `0`) and both reparse cleanly — zero eval-order risk. The three `getObserversCount() > 0` call-operand sites were NOT auto-rewritten by ANY tool: eslint's side-effect guard downgraded them to suggestion-only, astgrep's impure rule stayed detect-only, biome is detect-only. verdict: no unsafe fix exists; the conservative posture is correct (unsafe_autofixes: none).
- [x] astgrep: fix total false-negative (was broken on every TS site)
  - [x] add TypeScript rule variants so $A > $B / $A >= $B match .ts source (rules/*-ts.yml)
  - [x] regression test: TS parser does NOT match generic type-args (Array<T>, Promise<unknown>, WeakMap<…>) — rule-tests/*-ts-test.yml
  - [x] add Tsx rule variants so .tsx (JSX + arrow-generics) is covered (rules/*-tsx.yml)
  - [x] detection complete & correct across JS/TS/TSX; no missed sites (tri-tool review)
  - [x] astgrep: text-template fixes are behavior-preserving (tri-tool review) — over-parenthesization (e.g. (0) < (childVNode._depth)) is the unavoidable cost of a non-branching template; always-wrap is the only safe choice, so this is acceptable
  - [x] astgrep: restrict the autofixing rule to demonstrably-pure operands (literal/identifier/member); leave call/update operands detect-only (no fix) — the text template has NO side-effect guard, so it silently reorders side-effecting call operands. New snapshot had only pure operands so output was safe, but the rule is unsafe in general (commit 83b728ef2adf078851fb7b1068a47cf8e41cf5e4)
    - [x] astgrep: CLOSE the side-effect exclusion gap — the pure rules (no-greater-than{,-ts,-tsx}.yml) currently exclude ONLY `call_expression`/`update_expression` in either operand subtree, so a side-effecting operand that is NOT a call still auto-rewrites: e.g. `(x = expr) > 0` (assignment_expression) and `await pendingVar > 0` (await_expression w/o an inner call) both slip past the `not has any` guard and get silently reordered by the always-rewrite `($B) < ($A)` template. Add `assignment_expression` and `await_expression` to the excluded kinds list in all three pure rules (route them to the detect-only impure companions instead). DONE (commit 136e9e952a5cb0f40ae6c8502916b31c5f7ea71e): all three pure rules now exclude assignment_expression and await_expression so side-effecting non-call operands route to the detect-only impure companions instead of silently reordering.
  - [x] astgrep: keep autofixing rule language-scoped (TS/Tsx) so the always-parenthesize fix template does not leak into other languages without their own rule — ALREADY IMPLEMENTED (each rule sets language: TypeScript / Tsx; the .js base rule is separate)
  - [x] astgrep: keep the detect-only impure companion (no-greater-or-equal-ts-impure) WITHOUT a fix template — ALREADY IMPLEMENTED: the impure rule has no `fix:` key (matches call_expression/update_expression and only reports), so it cannot reorder side-effecting operands; the pure rule (no-greater-or-equal-ts) carries the `($B) <= ($A)` fix and excludes call/update operands
  - [x] astgrep: keep the `>` impure companion (no-greater-than-ts-impure) detect-only WITHOUT a fix template — VERIFIED on the `getObserversCount() > 0` snapshot: no-greater-than-ts-impure.yml has no `fix:` key and matches `$A > $B` where a subtree is call_expression/update_expression, so the three call-operand sites report but are never auto-reordered; the pure no-greater-than-ts.yml carries the `($B) < ($A)` fix and excludes those operands. Adding an auto-fix template here would be unsafe (no side-effect guard) — do NOT.
  - [ ] astgrep: fix line-reporting off-by-one ergonomics — a finding reported at 0-indexed line 178 corresponds to editor line 179. NOT in the rule YAML (ast-grep emits its own line numbers); fix belongs in downstream message/mapping normalization to 1-indexed. Low confidence; harness-level @leaf
  - [ ] astgrep: optionally PROMOTE pure-getter call comparisons to autofix — add a constraint to no-greater-or-equal-ts so `$A >= $B` matches when the only call operands are recognized pure getters (getTime/valueOf/getFullYear-family). NOT IMPLEMENTED: rule currently excludes ALL call_expression via `not has any call_expression`, so pure getters are sent to the detect-only impure companion. CAVEAT: ast-grep YAML cannot easily express a pure-call allowlist (no GritQL), so the safe fallback is to keep these detect-only rather than attaching the precedence-blind always-rewrite template; treat as low-confidence/optional @leaf
  - [ ] astgrep: optional fix-noise reduction (non-blocking)
    - [ ] reduce blanket parenthesization (`(curr) < (def.value)` style; also `(999) < (code)` / `(0) < (keys.length)` and `(0) < (this.state.dataUpdateCount + this.state.errorUpdateCount)` on the newer snapshots — note the redundant `(0)` around a bare literal is the most visible noise) — accept that astgrep CANNOT conditionally drop parens (text template cannot branch); only achievable by adding more specific atomic-operand pattern variants to omit parens for identifiers/literals/member-access and match eslint's cleaner output @leaf
    - [x] add an explicit edge-case rule/test for chained or nested comparisons — done (commit 0f75ef5: ternary `remaining > (matched ? 1 : 0)` + chained `a > b > c`, each wrapped as a single unit)
    - [ ] add edge-case detection rules/tests asserting >> / >>> shift operators and TS cast/generic contexts are NOT matched (guard against false positives, since the template approach is precedence-blind) @leaf
