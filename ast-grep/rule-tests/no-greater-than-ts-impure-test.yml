id: no-greater-than-ts-impure
valid:
  - "a > b"
  - "arr.length > 0"
  - "const xs: Array<number> = []"
invalid:
  - "foo() > b"
  - "a > bar()"
  - "i++ > b"
  - "(x = expr) > 0"
  - "async () => (await p) > 0"
