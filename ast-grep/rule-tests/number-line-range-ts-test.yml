id: number-line-range-ts
valid:
  # Already canonical: ascending number line, left alone.
  - "5 < x && x < 10"
  - "lo < x && x < hi"
  # Not a two-sided range over a single variable.
  - "a < b && c < d"
  - "x > 5 || x < 10"
  - "x > 5 && y < 10"
  # TS generics must NOT be mistaken for a range comparison.
  - "const xs: Array<number> = []"
  # Side-effecting operands are NOT auto-fixed here; the detect-only
  # `number-line-range-ts-impure` rule reports them instead.
  - "f() > 5 && f() < 10"
  - "x > 5 && x < f()"
invalid:
  # Both scrambled `&&` permutations -> canonical `lo < x && x < hi`.
  - "x > 5 && x < 10"
  - "x < 10 && x > 5"
  - "a > lo && a < hi"
